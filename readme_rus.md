# 🚀 Mockavior — что это и зачем он нужен

**Mockavior — это контрактно-ориентированный мок-сервис для HTTP и Kafka**, созданный для **детерминированного и честного интеграционного тестирования** без реальной инфраструктуры.

---

## 🔑 Ключевые возможности

### 📄 Один YAML — вся система
- Один файл (`mockapi.yml`) описывает **HTTP и Kafka**
- Контракт — **единственный источник истины**
- Никаких SDK, моков в тестах и кастомного кода

---

### 🔁 Горячая перезагрузка без поломок
- Контракт обновляется **на лету**
- In-flight запросы не ломаются
- Атомарная смена снапшота

---

### 🌐 HTTP-моки, ведущие себя как реальные сервисы
- Path-параметры, условия (`when`), приоритеты
- Подстановка значений (templates)
- **Тело ответа inline или из файла**
- **Фиксированные и случайные задержки**
- Ошибки и управляемый proxy (passthrough)

---

### 🧵 Kafka без Kafka
- Kafka — **side-effect контракта**
- Асинхронные сценарии с delay и repeat
- Payload inline или из файла
- **Kafka = bytes. Всегда.**
- Реальный брокер не нужен

---

### 📬 Admin Polling API
- peek / take / clear
- Режимы декодирования: `none | text | json`
- Raw bytes + decoded-представление
- Декодирование — только уровень представления

# Mockavior — Contract-driven Mock Platform

Mockavior — это **контрактно-ориентированная платформа мокирования HTTP API**, предназначенная для:

* локальной и dev-среды  
* интеграционного тестирования  
* эмуляции нестабильных / внешних сервисов 
* эмуляции кафки и polling
* controlled proxy (passthrough)

Платформа управляется **одним YAML-контрактом (`mockapi.yml`)**, поддерживает **горячую перезагрузку**, **условную маршрутизацию**,  **fallback-поведение**, **эмуляцию кафки** и ***поллинг сообщений**.

## 🧠 Основная идея

**Контракт — это единственный источник истины**

Mockavior:

* не хранит состояния запросов  
* не содержит бизнес-логики  
* не знает про конкретные API

Он **исполняет контракт**.

## 🧩 Архитектура для эмуляции REST контрактов(коротко)

HTTP request
   ↓
HttpTransportAdapter
   ↓
GenericRequest
   ↓
BehaviorEngine
   ↓
Router → Match → Behavior
   ↓
BehaviorResult
   ↓
HTTP response

Ключевые принципы:

* **Immutable ContractSnapshot**  
* **Atomic swap snapshot**  
* **In-flight requests не ломаются**  
* **Reload \= операция, а не side-effect**

## 🚀 Быстрый старт

##  Запуск Mockavior

В этом документе описаны доступные способы запуска **Mockavior** — от самого простого варианта без лишних действий до более гибких и настраиваемых сценариев.

Варианты отсортированы от **рекомендуемого / самого простого** к **наиболее минималистичному**.

---

### 1. Запуск без лишних действий (Prebuilt Docker Compose) ⭐ Рекомендуется

Это **рекомендуемый способ** начала работы.

Система запускается с использованием **готовых Docker-образов** из Docker Hub и включает:
- Mockavior
- Prometheus
- Grafana (с заранее настроенными дашбордами)

Не требуется:
- инструменты сборки
- Gradle
- клонирование репозитория

#### Требования
- Docker
- Docker Compose

#### Запуск
**Требование:** 
В текущей директории должна быть директория с файлом контракта сonfig/mockapi.yml 
```bash
curl -O https://github.com/unimilk456/mockavior/blob/main/mockavior-up.sh
chmod +x mockavior-up.sh
./mockavior-up.sh
```
Скрипт выполнит следующие действия:
- создаст минимальный контракт, если он отсутствует;
- запустит Mockavior, Prometheus и Grafana;
- предоставит доступ ко всему через localhost.


#### После запуска

- Mockavior: http://localhost:8080
- Grafana: http://localhost:3000
    - логин: `admin`
    - пароль: `admin`
- Prometheus: http://localhost:9090

Этот режим идеально подходит для:
- быстрого ознакомления
- демо
- локального тестирования с метриками
- пользователей, которым нужно, чтобы всё заработало сразу


### 1.1 Опциональный в одну команду
```
curl -fsSL https://raw.githubusercontent.com/unimilk456/mockavior/main/install.sh | bash
```
Этот скрипт загружает и запускает mockavior-up.sh.
При желании, перед запуском можете ознакомиться с ним.

---

### 2. Запуск из исходников (для разработчиков)

Этот вариант предназначен для пользователей, которые хотят:
- изучить исходный код
- изменить метрики или дашборды
- настроить конфигурацию под себя
- внести вклад в проект

Система собирается из **локальных исходников**.

#### Требования
- Git
- Docker
- Docker Compose

#### Запуск

```bash
git clone <repository-url>
cd mockavior
docker compose up
```

В этом режиме:
- Mockavior собирается из исходников
- дашборды и метрики можно свободно изменять
- Grafana provisioning включён для разработки

---

### 3. Запуск одного JAR-файла (без Grafana и Prometheus)

В этом варианте запускается **только приложение Mockavior**.

Метрики по‑прежнему доступны, но Prometheus и Grafana автоматически не запускаются.

#### Требования
- Java 21+

#### Запуск

```bash
java -jar mockavior.jar
```

#### Endpoint метрик

```
GET /actuator/prometheus
```

Этот режим полезен для:
- CI‑окружений
- встраивания в существующие системы
- минималистичных runtime‑сценариев

---

### 4. Один Docker-образ (только Mockavior)

В этом варианте Mockavior запускается как **один Docker‑контейнер**, без Grafana и Prometheus.

#### Требования
- Docker

#### Запуск

```bash
docker run -p 8080:8080   -v $(pwd)/config:/app/config   unisoft123/mockavior:latest
```

Метрики по‑прежнему доступны по адресу:

```
/actuator/prometheus
```

Этот режим полезен, когда:
- Prometheus/Grafana уже запущены отдельно
- Mockavior развёртывается как часть более крупной системы

---

### Как выбрать подходящий вариант

| Сценарий | Рекомендуемый вариант |
|--------|----------------------|
| Быстрое демо / первый запуск | Prebuilt Docker Compose |
| Разработка / кастомизация | Docker Compose из исходников |
| Минимальный runtime | Standalone JAR |
| Существующий observability‑стек | Один Docker‑образ |


## 📄 Контракт mockapi.yml
Mockavior requires a contract file at startup.
The container will not start without a mounted contract.

### Минимальный пример

```
version: 1  
settings:  
  mode: STRICT  
  defaultStatus: 404

endpoints: 
  - id: health 
    request:  
      method: GET  
      path: /health  
    response:  
      type: mock  
      status: 200  
      body: "OK"
```

## 🔀 Routing

### Поддерживается
* `literal paths`  
   `/health`
* `parameterized paths`  
   `/users/{id}`  
* `приоритеты (priority)`  
* `условия (when)`
* `фиксированные и задержки с указание величины случайного интервала ответа (fixed + random)`
* `загрузка тела ответа через файл и напрямую в контракте (bodyFile + body)`

### Пример с параметрами

```
- id: get-user  
  priority: 10  
  request:  
    method: GET  
    path: /users/{id}  
  response:  
    type: mock  
    status: 200  
    body:  
      id: "{id}"
```

Запрос:  
`GET /users/123`

Ответ:  
`{`  
  `"id": "123"`  
`}`

## 🧠 Условия (when)

### Query-параметры

```
when: 
  query:  
    active: true
```

`GET /users/123?active=true`


### Сопоставление query-параметров с повторяющимися значениями

Mockavior поддерживает расширенное сопоставление **повторяющихся query-параметров**, например:

```
/test?tag=as&tag=ab
```

Это позволяет гибко маршрутизировать запросы на основе **наличия параметра**, **точных значений** и **условий над множествами значений**.

---

#### Поддерживаемые конструкции DSL

#### 1. Wildcard — наличие параметра

```yaml
tag: "*"
```

Совпадает с **любым запросом, в котором присутствует параметр `tag`**, независимо от его значения.

| Запрос            | Совпадение |
|-------------------|------------|
| `?tag=x`          | ✅         |
| `?tag=as&tag=ab` | ✅         |
| *(без tag)*       | ❌         |

---

### 2. Точное совпадение значения

```yaml
tag: "as"
```

Совпадает, если **хотя бы одно** из повторяющихся значений параметра равно `"as"`.

| Запрос            | Совпадение |
|-------------------|------------|
| `?tag=as`         | ✅         |
| `?tag=as&tag=b`   | ✅         |
| `?tag=b`          | ❌         |

---

### 3. `any` — должно совпасть хотя бы одно значение

```yaml
tag:
  any: ["as", "ab"]
```

Совпадает, если **хотя бы одно** из указанных значений присутствует в запросе.

| Запрос       | Совпадение |
|--------------|------------|
| `?tag=ab`    | ✅         |
| `?tag=as`    | ✅         |
| `?tag=sc80`  | ❌         |

---

### 4. `all` — должны присутствовать все значения

```yaml
tag:
  all: ["as", "ab"]
```

Совпадает только если **все обязательные значения** присутствуют (порядок не имеет значения).

| Запрос            | Совпадение |
|-------------------|------------|
| `?tag=as&tag=ab` | ✅         |
| `?tag=as`        | ❌         |
| `?tag=ab`        | ❌         |

---

### Приоритет сопоставления

Если одному запросу потенциально соответствуют несколько эндпоинтов, Mockavior применяет следующий **порядок приоритетов** (от высокого к низкому):

1. `all`
2. `any`
3. точное совпадение (`tag: "as"`)
4. wildcard (`tag: "*"`)

**Побеждает первое совпадение с наивысшим приоритетом**.

---

### Поведение fallback (Вариант A — по умолчанию)

Если query-параметр присутствует, **должен совпасть хотя бы один эндпоинт**.

Это означает:
- если `all`, `any` или точное совпадение не сработали,
- но параметр присутствует в запросе,
- тогда используется wildcard (`"*"`), если он определён.

**Пример**

При наличии следующих эндпоинтов:
```
- when:
    query:
      tag:
        all: ["as", "ab"]
- when:
    query:
      tag:
        any: ["as", "ab"]
- when:
    query:
      tag: "as"
- when:
    query:
      tag: "*"
```

| Запрос             | Сработавшее правило |
|--------------------|---------------------|
| `?tag=as&tag=ab`  | `all`               |
| `?tag=ab`         | `any`               |
| `?tag=as`         | `equals`            |
| `?tag=sc80`       | `*`                 |
| *(без tag)*       | ❌ 404               |

---

### Строгое поведение (без wildcard)

Если **wildcard-эндпоинт не определён**, и ни одно правило не совпало:

- запрос завершается с **404 NOT FOUND**
- это позволяет создавать **строгие контракты**

Полезно для изолированного тестирования семантики `any` / `all`.

---

### Обоснование дизайна

Такой дизайн гарантирует:

- наличие параметра никогда не игнорируется молча
- маршрутизация предсказуема и объяснима
- fallback-поведение явно задано контрактом
- строгий режим остаётся доступным при необходимости

### Headers

```when:  
  headers:  
    X-Role: admin  
    X-Debug: "*"
```

\* → параметр должен присутствовать  
заголовки case-insensitive

### Приоритет при конфликте

Если несколько маршрутов совпадают:

* выше priority  
* when должен выполниться  
* первый совпавший — выигрывает

## 🎭 Типы response

### 1️⃣ mock

```
response:  
  type: mock  
  status: 200  
  headers:  
    X-User-Id: "{id}"  
  body:  
    id: "{id}"
```
Поддерживается template replacement:

* path params  
* query params  
* headers (в body / headers)

#### Загрузка body из файла (bodyFile)
Вместо inline-body можно указать файл с телом ответа.
```
response:
  type: mock
  status: 200
  bodyFile: responses/user.json
```

**Правила:**
- путь относительно директории контракта
- файл читается как bytes
- поддерживаются JSON, text, binary
- bodyFile имеет приоритет над body

Если указаны оба поля:
```
body: { ignored: true }
bodyFile: responses/user.json
```
→ будет использован **bodyFile**.

#### Задержки ответа (delay)

Mockavior поддерживает фиксированные и случайные задержки для HTTP-ответов.

_Фиксированная задержка:_
```
response:
  type: mock
  status: 200
  delay: 500ms
  body:
    ok: true

```

Ответ будет отправлен через **500 мс** после получения запроса.

**Случайная задержка**
```
response:
  type: mock
  status: 200
  delay:
    random:
      min: 200ms
      max: 800ms
  body:
    ok: true
```
Фактическая задержка будет выбрана **случайно в диапазоне** [200ms, 800ms]

**Комбинированная задержка (fixed + random)**
```
response:
  type: mock
  status: 200
  delay:
    fixed: 300ms
    random:
      min: 100ms
      max: 400ms
  body:
    ok: true
```
Фактическая задержка: `fixed + random → [400ms … 700ms]`

### 2️⃣ error

```
response:  
  type: error  
  status: 500
```

Используется для:

* forced failures  
* chaos testing  
* negative scenarios

### 3️⃣ proxy

```
response: 
  type: proxy
```

## 🔁 Fallback behavior

Fallback применяется только если ни один endpoint не совпал.

### STRICT (default)

```
settings:  
  mode: STRICT  
  defaultStatus: 404
```

### PASSTHROUGH

```
settings:  
  mode: PASSTHROUGH  
  proxy:  
    baseUrl: http://httpbin.org
```

`GET /status/418 → forwarded to httpbin.org/status/418`

## 🔄 Reload контракта

### Автоматически

* при изменении mockapi.yml  
* через WatchService  
* без остановки сервиса

### Вручную

`POST /__mockavior__/reload`

Ответ:  
```
{  
  "status": "SUCCESS",  
  "source": "/path/to/mockapi.yml",  
  "snapshotVersion": 12  
}
```

## 🛠 Admin API

### Получить текущий контракт

`GET /__mockavior__/contract`

Headers:

* Mockavior-Contract-Version: 12  
* Content-Type: application/yaml

Body:  
`version: 1`  
`…`

### Обновить контракт (PUT)

`PUT /__mockavior__/contract`  
`If-Match: 12`  
`Content-Type: text/plain`

Body:  
`<содержимое файла mockapi.yml>`

Возможные ответы  
Status	Meaning  
200	OK  
409	Version conflict  
400	Validation error

### Optimistic Locking

* Full replace  
* защита от перезаписи изменений  
* версия берётся из Mockavior-Contract-Version

### Проверка контракта (validate)

`POST /__mockavior__/contract/validate`  
`Content-Type: text/plain`

Ответ:  
```
{  
  "status": "VALID",  
  "message": "Contract validation successful"  
}
```  
или  
```
{  
  "code": "VALIDATION_ERROR",  
  "message": "Unknown response.type: foo"  
}
```

❌ ErrorResponse (единый формат)

```
{  
  "code": "VERSION_CONFLICT",  
  "message": "Contract version mismatch",  
  "currentVersion": "13"  
}
```


# Mockavior — Kafka Emulation & Polling

## 📌 Назначение

Kafka-эмуляция в Mockavior предназначена **не для замены Kafka**, а для:

- детерминированных интеграционных тестов
- воспроизводимых сценариев
- проверки реакций consumer‑ов
- контрактного тестирования event‑driven систем

Это **event store + scheduler**, управляемый контрактом.

---

## 🧠 Ключевая идея

> Kafka — это side‑effect контракта

HTTP → Kafka в Mockavior **не связаны напрямую**.  
Kafka-сценарии описываются в контракте и исполняются асинхронно.

---

## 🧩 Архитектура Kafka-эмуляции

```
ContractSnapshot
   └── kafka.scenarios
         └── KafkaScenario
               └── KafkaMessage (topic, key, value, delay, repeat)
                        ↓
               ScenarioExecutionRunner (async)
                        ↓
               InMemoryKafkaStore
                        ↓
               KafkaPollController
```

### Основные компоненты

- `KafkaScenario` — описание сценария
- `ScenarioExecutionRunner` — асинхронный исполнитель
- `RuntimeScheduler` — управляет задержками
- `InMemoryKafkaStore` — потокобезопасное хранилище
- `KafkaPollController` — HTTP polling API

---

## 📄 Контракт: kafka section

### Пример

```yaml
kafka:
  scenarios:
    user-events:
      repeat: 1
      messages:
        - topic: user.created
          key: user-1
          value:
            id: 1
            name: John
          delay: 0

        - topic: user.updated
          key: user-1
          value:
            name: John Updated
          delay: 1000
```
### Задержки Kafka-сообщений
Каждое Kafka-сообщение может иметь задержку перед публикацией.

**Фиксированная задержка**
```
- topic: user.created
  key: user-1
  value:
    id: 1
  delay: 1000ms
```
Сообщение будет опубликовано **через 1 секунду**.

**Случайная задержка**
```
delay:
  random:
    min: 300ms
    max: 800ms
```

**Комбинированная задержка**
```
delay:
  fixed: 500ms
  random:
    min: 200ms
    max: 600ms
```
Фактическая задержка: `500ms + [200ms...600ms]`.

### Семантика

| Поле | Значение |
|-----|---------|
| topic | Kafka topic |
| key | message key |
| value | payload (JSON) |
| delay | задержка перед publish (ms) |
| repeat | сколько раз отправить сообщение |


---

### Загрузка value из файла (valueFile)
Kafka-сообщения могут загружать payload из файла, аналогично HTTP-response.
```
kafka:
  scenarios:
    user-events:
      messages:
        - topic: user.created
          key: user-1
          valueFile: values/user-created.json
```

Семантика:
- файл читается как bytes
- путь относительно контракта
- valueFile имеет приоритет над value
- Kafka всегда хранит bytes, без интерпретации

Если указаны оба поля:
```
value:
  id: 1
valueFile: values/user.json
```
→ будет использован **valueFile.**

## ▶️ Запуск сценария

```
POST /__mockavior__/kafka/start/{scenarioId}
```

Ответ:

```json
{
  "executionId": "uuid",
  "scenarioId": "user-events",
  "state": "RUNNING"
}
```

📌 Сценарий:

- исполняется **асинхронно**
- не блокирует HTTP
- может публиковать сообщения с delay

---

## ⏹ Остановка сценария

```
POST /__mockavior__/kafka/stop/{executionId}
```

Используется для:

- cleanup
- аварийного завершения
- тестовых сценариев

---

## 📬 Polling API (основное)

Decode режимы (Admin API)
Kafka в Mockavior всегда хранит bytes.
Декодирование выполняется на уровне Admin API.

| decode         | Описание                  |
| -------------- | ------------------------- |
| none (default) | только raw bytes (Base64) |
| text           | UTF-8 строка              |
| json           | JSON (если возможно)      |

**Пример**
```
GET /__mockavior__/kafka/poll/user.created?decode=json
```
**Ответ:**
```
{
  "value": {
    "raw": "eyJpZCI6MSwibmFtZSI6IkFsaWNlIn0=",
    "decoded": { "id": 1, "name": "Alice" },
    "decode": "json",
    "source": "inline"
  }
}
```
Важно:
- Kafka-store никогда не хранит decoded
- decoded — это presentation concern
- raw всегда доступен

### Peek (неразрушающий)

```
GET /__mockavior__/kafka/poll/{topic}
```

Ответ:

```json
{
  "topic": "user.created",
  "count": 1,
  "messages": [
    {
      "topic": "user.created",
      "key": "user-1",
      "value": {
        "raw": "eyJpZCI6MSwibmFtZSI6IkFsaWNlIn0=",
        "decoded": { "id": 1, "name": "Alice" },
        "decode": "json",
        "source": "inline"
      }
    }
  ]
}
```

📌 Сообщения **не удаляются**.

---

### Take (разрушающий)

```
POST /__mockavior__/kafka/poll/{topic}/take
```

Ответ:

```json
{
  "topic": "user.created",
  "key": "user-1",
  "value": { "id": 1 },
  "repeat": 1,
  "delay": "PT0S"
}
```

- сообщение **удаляется**
- FIFO
- если сообщений нет → `204 No Content`

---

### Clear topic

```
POST /__mockavior__/kafka/poll/{topic}/clear
```

Ответ:

```json
{ "cleared": true }
```

---

## 🔁 Асинхронность

- все Kafka‑сообщения публикуются через `RuntimeScheduler`
- используется delay из контракта
- сценарии могут работать параллельно
- store потокобезопасный (`ConcurrentHashMap + Queue`)

---

## 🧪 Интеграционные тесты

Типовой сценарий:

1. загрузить контракт
2. `POST /kafka/start/{scenario}`
3. `sleep()` (или polling loop)
4. `POST /poll/{topic}/take`
5. assert payload
6. `GET /poll/{topic}` → count == 0

Пример:

```
Map<String, Object> msg =
  client.post()
        .uri("/__mockavior__/kafka/poll/user.created/take")
        .retrieve()
        .bodyToMono(Map.class)
        .block();

assertThat(msg.get("key")).isEqualTo("user-1");
```

---

## Метрики и наблюдаемость

Данный проект предоставляет расширенный набор метрик через **Prometheus** и готовые к использованию **Grafana-дашборды** для наблюдаемости.

---

### Обзор

Приложение экспортирует метрики по адресу:

```
GET /actuator/prometheus
```

Prometheus опрашивает этот endpoint, а Grafana визуализирует данные с использованием заранее настроенных дашбордов.

Метрики сгруппированы по следующим категориям:
- Жизненный цикл приложения
- HTTP и маршрутизация
- Снэпшоты
- Исполнители и потоки
- JVM и GC
- Система и процесс

---

### Пользовательские метрики приложения (`mockavior_*`)

#### HTTP и маршрутизация

| Метрика | Тип | Описание |
|------|------|-------------|
| `mockavior_http_requests_total` | counter | Общее количество HTTP-запросов |
| `mockavior_http_request_duration_seconds_*` | summary / gauge | Полная длительность HTTP-запросов |
| `mockavior_routing_match_time_seconds_bucket` | histogram | Время сопоставления маршрутов |
| `mockavior_routing_match_time_seconds_max` | gauge | Максимальное время сопоставления маршрута |
| `mockavior_routing_matched_total` | counter | Количество запросов, сопоставленных с маршрутом |
| `mockavior_routing_fallback_total` | counter | Количество запросов, направленных в fallback |

Перцентили задержек вычисляются в Prometheus следующим образом:

```
histogram_quantile(0.95,
  sum by (le) (
    increase(mockavior_routing_match_time_seconds_bucket[15m])
  )
)
```

---

#### Снэпшоты

| Метрика | Тип | Описание |
|------|------|-------------|
| `mockavior_snapshot_active` | gauge | Текущий активный снэпшот |
| `mockavior_snapshot_in_flight` | gauge | Снэпшоты в процессе обработки |
| `mockavior_snapshot_lifetime_seconds_*` | summary / gauge | Время жизни снэпшота |
| `mockavior_snapshot_retire_wait_seconds_*` | summary / gauge | Задержка вывода снэпшота |

---

### Жизненный цикл приложения

| Метрика | Тип | Описание |
|------|------|-------------|
| `application_started_time_seconds` | gauge | Время запуска приложения |
| `application_ready_time_seconds` | gauge | Время до готовности приложения |
| `process_uptime_seconds` | gauge | Время работы JVM |
| `process_start_time_seconds` | gauge | Время запуска JVM (timestamp) |

Метрика `application_started_time_seconds` используется для определения перезапусков приложения.

---

### Исполнители и потоки

| Метрика | Тип | Описание |
|------|------|-------------|
| `executor_active_threads` | gauge | Активные потоки исполнителя |
| `executor_queued_tasks` | gauge | Задачи в очереди |
| `executor_queue_remaining_tasks` | gauge | Оставшаяся ёмкость очереди |
| `executor_pool_size_threads` | gauge | Текущий размер пула |
| `executor_pool_max_threads` | gauge | Максимальный размер пула |

---

### JVM и сборка мусора

| Метрика | Тип | Описание |
|------|------|-------------|
| `jvm_memory_used_bytes` | gauge | Используемая память JVM |
| `jvm_memory_committed_bytes` | gauge | Выделенная память JVM |
| `jvm_memory_max_bytes` | gauge | Максимальная память JVM |
| `jvm_gc_pause_seconds_*` | summary / gauge | Паузы сборки мусора |
| `jvm_gc_overhead_percent` | gauge | Нагрузка GC на CPU |
| `jvm_threads_live_threads` | gauge | Активные потоки JVM |

---

### Метрики системы и процесса

| Метрика | Тип | Описание |
|------|------|-------------|
| `process_cpu_usage` | gauge | Использование CPU JVM |
| `system_cpu_usage` | gauge | Использование CPU системы |
| `system_load_average_1m` | gauge | Средняя нагрузка за 1 минуту |
| `disk_free_bytes` | gauge | Свободное место на диске |
| `disk_total_bytes` | gauge | Общий объём диска |

---

### Grafana-дашборды

Следующие дашборды создаются автоматически:

1. **Mockavior · Overview**  
   Общий обзор состояния и задержек

2. **Mockavior · Routing & Latency**  
   Гистограммы маршрутизации и перцентили

3. **Mockavior · HTTP Runtime**  
   Частота запросов и длительности

4. **Mockavior · Executors & Threads**  
   Пулы потоков и очереди

5. **Mockavior · JVM & GC**  
   Память, GC и внутренние метрики JVM

6. **Mockavior · Lifecycle & Stability**  
   Запуск, аптайм, перезапуски

Дашборды провиженятся через Grafana provisioning и хранятся в репозитории.

---

### Примечания

- Для гистограмм используется `increase()` — оптимально для сред с низкой нагрузкой / dev.
- Дашборды версионируются и управляются через provisioning.
- Layout следует 24-колоночной сетке.

---

### Быстрая проверка

```bash
curl http://localhost:8080/actuator/prometheus | grep mockavior_
```

Если метрики возвращаются и target Prometheus имеет статус **UP**, наблюдаемость настроена корректно.

---

## 🧱 Гарантии

✔ deterministic order per topic  
✔ no shared state outside store  
✔ no real Kafka dependency  
✔ reproducible tests  
✔ snapshot‑safe (reload не ломает runner)

---

## ❌ Осознанные ограничения

- нет partitions
- нет consumer groups
- нет offset management
- нет retention
- нет exactly-once

> Это **test double**, а не broker.

---

## 🧭 Когда использовать

✔ contract testing  
✔ async workflows  
✔ saga testing  
✔ consumer simulation  
✔ CI pipelines

❌ performance testing  
❌ real Kafka behavior validation

---

## 🧠 Философия

> Kafka — это не инфраструктура, а контракт событий.

Mockavior делает события **частью API‑контракта**.

Kafka = bytes. Всегда.
Интерпретация данных — ответственность клиента.

Mockavior намеренно разделяет:
- хранение (raw bytes)
- представление (decoded via Admin API)

Это устраняет:
- ложные ожидания форматов
- магические преобразования
- неявную сериализацию

И делает Kafka-контракты честными и переносимыми.



### 🧱 Границы текущего MVP

✅ Уже реализовано:

* HTTP mock  
* proxy  
* error responses  
* conditional routing  
* hot reload  
* optimistic locking  
* immutable snapshots  
* admin API

❌ Пока нет (осознанно):

* partial merge контракта  
* UI  
* auth / RBAC  
* rate limiting  
* metrics / tracing  
* OpenAPI export

